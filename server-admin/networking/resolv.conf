# Q3: sample /etc/resolv.conf on web01

nameserver 1.1.1.1
nameserver 8.8.8.8
search internal.example.com
options timeout:2 attempts:3

# Pair with routes.txt in this same directory and ask the candidate to walk
# through, step by step, what happens when they run `curl example.com` on
# this host: name resolution first, then which route table entry carries
# the resulting TCP connection.
